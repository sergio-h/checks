name: trigger gitlab job
on:
    push:
      branches: [ main ]
    pull_request:
      branches: [ main ]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: trigger Job
      uses: appleboy/gitlab-ci-action@master
      with:
        host: "https://gitlab.com/sergio.h/ci-github"
        token: "f943d819a39d860c5d2c60258a6604"
        project_id: 22288234
